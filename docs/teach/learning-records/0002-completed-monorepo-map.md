# 完成 Lesson 0001：Monorepo 地图

用户完成了第一课自测，能回答「二开时代码该放哪个 workspace」——共享契约在 `data-provider`，新业务逻辑在 `packages/api`，DB 在 `data-schemas`，UI 在 `client`，legacy `/api` 只做薄路由。

## Evidence

用户回复「完成了」。

## Implications

- 可进入 Lesson 0002：以 `getStartupConfig` 为样本，掌握 data-provider 三件套（types · endpoints · data-service）及端到端 trace 方法。
- 前端侧概念可快讲；重点放在「契约层如何连接前后端」这一桥梁技能。
