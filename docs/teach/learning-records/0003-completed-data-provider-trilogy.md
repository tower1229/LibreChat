# 完成 Lesson 0002：data-provider 三件套

用户完成了第二课自测，能掌握 `getStartupConfig` 从 types → endpoints → data-service → request → Express handler 的 trace 路径，并理解二开时「契约层先行」的修改顺序。

## Evidence

用户在对话中确认「完成了 0002」。

## Implications

- 可进入 Lesson 0003：`client/src/data-provider/` 的 React Query hook 层，用同一 `useGetStartupConfig` 样本把契约层接到组件。
- Trace 技能已建立，后续端到端课（消息流、新功能样板）可要求用户先自行 trace 再讲解。
